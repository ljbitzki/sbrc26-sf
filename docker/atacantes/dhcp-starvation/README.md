# Ataque "DHCP REQUEST flood - DHCP Startvation"

> Executa 1000 de requisições DHCP REQUEST com endereços MAC aleatórios

### Execução do ataque:
```
docker run --rm -d --name sbrc26-ataque-dhcp-starvation sbrc26-ataque-dhcp-starvation:latest
```