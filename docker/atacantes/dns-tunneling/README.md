# Ataque "DNS Tunneling"

> Executa 200 tentativas de resolução de nomes de domínios aleatórios com até 50 caracteres.

### Execução do ataque:

```
docker run --rm -d --name sbrc26-ataque-dns-tunneling sbrc26-ataque-dns-tunneling:latest
```